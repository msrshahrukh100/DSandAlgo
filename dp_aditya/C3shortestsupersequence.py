# find the length of shortest supersequence
# supersequence - the string which contains both the strings as subsequence


'''
    Find the length of common subsequence
    than return len(a) + len(b) - length of lcs

'''
